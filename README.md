# Leggen

A Minecraft 26.2 mod targeting Fabric and NeoForge from a shared common source set.

Use Java 25 and run `./gradlew build` to build all supported platforms. Most code belongs in `common`; loader-specific integrations belong in `fabric` or `neoforge`.
